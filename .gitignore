# Pipeline outputs (now in site/data/ — tracked in git for deployment)
# site/data/*.json are committed so Cloudflare Pages can serve them
# The pipeline overwrites these files in-place on each run.

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.env

# Logs
*.log
pipeline.log

# OS
.DS_Store
Thumbs.db

# Lock files
.pipeline.lock

# Node (if any future frontend build)
node_modules/
